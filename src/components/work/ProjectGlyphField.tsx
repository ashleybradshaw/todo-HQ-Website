/** Quiet mono field behind a work detail page. Motion is CSS-only. */
export function ProjectGlyphField() {
  return (
    <div
      aria-hidden="true"
      className="project-glyph-field pointer-events-none absolute inset-0 z-0"
    />
  );
}
